[package]
name = "recall-native-db"
version = "0.1.0"
edition = "2021"

[dependencies]
rusqlite = { version = "0.40.1", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
